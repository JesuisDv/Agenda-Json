// URL base del API. Para producción, cámbiala por la URL de tu backend.
// Ejemplo: 'https://tu-app.render.com' o '' si el front se sirve desde el mismo backend
window.API_BASE_URL = window.API_BASE_URL || 'http://localhost:3000'
